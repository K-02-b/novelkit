# ---------------------------------------------------------------------------
# 凭据与密钥：绝对不要提交
# ---------------------------------------------------------------------------
.env
.env.*
*.env
!*.env.example
key.txt

# ---------------------------------------------------------------------------
# 运行产物
# ---------------------------------------------------------------------------
log/
*.log
nohup.out
.backups/
# 「删除整本书」把整部作品移到这里（不是真删，可恢复），不入库
.trash/
__pycache__/
*.py[cod]
.pytest_cache/

# ---------------------------------------------------------------------------
# 虚拟环境
# ---------------------------------------------------------------------------
.venv/
venv/

# ---------------------------------------------------------------------------
# 小说数据（体积大，且属于受版权保护的私有内容）
#   导入的作品一律放在 works/ 下：一个目录 = 一部作品。
#   该目录整体不入库，只保留一个占位文件；想换位置用 NOVELKIT_WORKSPACE。
# ---------------------------------------------------------------------------
works/*
!works/.gitkeep

# 旧版本把作品直接放在仓库根目录，下面的衍生文件规则继续生效，便于平滑迁移
download/
bak/
log.txt
origin.txt
*_origin.txt
*_translated.txt
*_back.txt
*_review.txt
*_refine.json
glossary_*.json
glossary_conflicts.jsonl
global_glossary_tracker.json

# ---------------------------------------------------------------------------
# 本机用户配置（每位使用者各有一套，不入库）
# ---------------------------------------------------------------------------
webpanel/panel.env
webpanel/tools.json
webpanel/prompt_snippets.json

# ---------------------------------------------------------------------------
# 第三方工具：本项目不附带、不内嵌（安装方式见 deploy/cloud.md）
# ---------------------------------------------------------------------------
tools/tomato/
