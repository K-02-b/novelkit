# NovelKit 环境配置示例。
#
#   cp .env.example .env && chmod 600 .env
#
# .env 已被 .gitignore 忽略，请勿提交。
# 面板「环境配置」页可以可视化地改这三个键（改前自动备份为 .env.bak，改后权限 600）。

# --- 模型 API ---------------------------------------------------------------
# 默认 DeepSeek；任何 OpenAI 兼容服务都可以，改地址与模型 ID 即可。
API_KEY=sk-你的密钥
API_BASE_URL=https://api.deepseek.com
API_MODEL=deepseek-flash

# --- 作品工作区（可选）------------------------------------------------------
# 导入的作品默认放在 <项目根>/works/。想放到数据盘再取消注释。
# NOVELKIT_WORKSPACE=/data/novels
